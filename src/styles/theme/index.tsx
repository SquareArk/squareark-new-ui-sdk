import { colorPalette } from './color-palette';
import { typography } from './typography';

export const CommonTheme = {
  colorPalette: {
    ...colorPalette,
  },
  typography: {
    ...typography,
  },
};
